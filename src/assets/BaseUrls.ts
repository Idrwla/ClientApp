const localhost = 'https://localhost:44323/api/';

export const cityUrl = localhost + 'city';

export const bankUrl = localhost + 'bank';

export const partnerUrl = localhost + 'Partner';

export const ticketUrl = localhost + 'Ticket';

export const Types = localhost + 'Types';

export const userUrl = localhost + 'User';

export const singInUrl = localhost + 'Auth';
